# ─────────────────────────────────────────────
# Keycloak-Datenbank
# ─────────────────────────────────────────────
KC_DB_NAME=keycloak
KC_DB_USER=keycloak
KC_DB_PASSWORD=keycloak
KC_DB_PORT=5433

# ─────────────────────────────────────────────
# Applikations-Datenbank (PostgreSQL)
# ─────────────────────────────────────────────
APP_DB_NAME=cookieclicker
APP_DB_USER=postgres
APP_DB_PASSWORD=postgres
APP_DB_PORT=5434

# ─────────────────────────────────────────────
# Keycloak
# ─────────────────────────────────────────────
KEYCLOAK_ADMIN=admin
KEYCLOAK_ADMIN_PASSWORD=admin
KEYCLOAK_PORT=8180
KEYCLOAK_REALM=cookieclicker
KEYCLOAK_CLIENT_ID=cookieclicker-client
KEYCLOAK_CLIENT_SECRET=5SSSXJd0Q9sYlSdMNVC8LifE7KqufRau

# ─────────────────────────────────────────────
# Service-URLs (lokale Entwicklung)
# ─────────────────────────────────────────────
KEYCLOAK_URL=http://localhost:8180
AUTH_SERVICE_URL=http://localhost:8081
PERSISTENCE_SERVICE_URL=http://localhost:8082

# ─────────────────────────────────────────────
# Eureka / Gateway (lokale Entwicklung)
# ─────────────────────────────────────────────
EUREKA_URL=http://localhost:8761
EUREKA_PORT=8761
EUREKA_HOST=localhost
GATEWAY_PORT=8090

# ─────────────────────────────────────────────
# Datenbank Host/Port (für persistance-service)
# ─────────────────────────────────────────────
DB_HOST=localhost
DB_PORT=5434

# ─────────────────────────────────────────────
# Netzwerk / LAN-Zugang
# ─────────────────────────────────────────────
# Leer lassen für lokalen Zugang (localhost).
# Für LAN-Zugang: eigene IP-Adresse eintragen, z.B. 192.168.1.100
HOST_IP=10.10.18.106

# ─────────────────────────────────────────────
# Frontend (lokale Entwicklung)
# ─────────────────────────────────────────────
NUXT_PUBLIC_API_BASE=http://localhost:8090
NUXT_PUBLIC_WS_BASE=ws://localhost:8090
